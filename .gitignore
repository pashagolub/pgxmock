# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Coverage profiles: *.out covers `go test -coverprofile=coverage.out`,
# *.cov matches profile.cov, the name the CI workflow writes.
*.out
*.cov

# Visual Studio Code internal folder
.vscode

# Packages output folder
dist

# delve debugger file
debug